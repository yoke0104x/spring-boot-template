server:
  port: 14500
  servlet:
    context-path: /api

spring:
  datasource:
    url: jdbc:mysql://mysql.sqlpub.com:3306/sqlpub_test_yoke?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=UTC
    username: yoke0104
    password: 4N9vOdUAOQFvguVl
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    default-property-inclusion: non_null

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
    enabled: true
  api-docs:
    path: /v3/api-docs
    enabled: true
  group-configs:
    group: "default"
    paths-to-match: /**
    packages-to-scan: xyz.content.template

knife4j:
  enable: true
  setting:
    language: zh_cn
    swagger-model-name: 实体列表